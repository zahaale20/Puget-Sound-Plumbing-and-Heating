# Public build-time values baked into the client bundle by Vite.
# These are NOT secrets — they are visible in the shipped JS either way.
# Real secrets belong in Vercel env vars / .env.local, never here.
VITE_SUPABASE_STORAGE_URL=https://hyxqrhttputdkefadnrf.supabase.co
VITE_API_BASE_URL=https://pspah-api.vercel.app
