[pytest]
testpaths = server/tests
python_files = test_*.py
addopts = -q --strict-markers --disable-warnings --cov=server --cov-report=term-missing --cov-report=xml:server/coverage.xml --cov-fail-under=60
